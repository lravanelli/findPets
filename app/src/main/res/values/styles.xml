<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- FullScreen theme. -->
    <style name="FullScreen" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="editText">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:layout_height">@dimen/cinquenta</item>
        <item name="android:layout_marginLeft">@dimen/cinquenta</item>
        <item name="android:layout_marginRight">@dimen/cinquenta</item>
        <item name="android:textColorHint">@color/colorHint</item>

    </style>

    <style name="texItem">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textColorHint">@color/colorHint</item>
    </style>

    <style name="textViewPad">
        <item name="android:padding">@dimen/vinte</item>
        <item name="android:textColor">@color/textview_color</item>
    </style>

    <style name="textView">
        <item name="android:textColor">@color/textview_color</item>
    </style>

    <style name="textViewTopMargin">
        <item name="android:layout_marginTop">@dimen/quinze</item>
        <item name="android:textColor">@color/textview_color</item>
    </style>

    <style name="button">
        <item name="android:layout_marginTop">@dimen/vinte</item>
        <item name="android:layout_marginLeft">@dimen/setenta</item>
        <item name="android:layout_marginRight">@dimen/setenta</item>

    </style>

</resources>
